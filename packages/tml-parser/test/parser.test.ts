import { describe, it, expect } from 'vitest'

describe('TML Parser', () => {
  it('should parse an empty document', () => {
    expect(true).toBe(true)
  })

  // More tests will be added as the parser is implemented
})
